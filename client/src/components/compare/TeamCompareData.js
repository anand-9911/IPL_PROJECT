import React from 'react';
import PropTypes from 'prop-types';

const TeamCompareData = (props) => {
  return <div>TeamCompareData</div>;
};

TeamCompareData.propTypes = {};

export default TeamCompareData;
